{
  "name": "lasotuvi-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker API - nhận ngày giờ sinh, lập lá số và trả về luận giải (dữ liệu luận giải nằm riêng ở phía server)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "tuvi-neo": "^1.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.6.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
